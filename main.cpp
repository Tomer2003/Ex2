#include <iostream>
#include "ClassMatrix.hpp"
/*
void printMatrix(const Matrix::Matrix& matrix){
    for(auto j = matrix.matrixGetHeight() - 1; j >= 0; --j){
        for(auto i = 0; i < matrix.matrixGetWidth(); ++i){
            std::cout << matrix(j, i);
        }   
        std::cout << std::endl; 
    }
}
*/
int main() {
    matrix::Matrix a(1, 1);
  /*  A.matrixSetValue(0, 0, 1);
    A.matrixSetValue(1, 0, 5);
    A.matrixSetValue(0, 1, 1);
    A.matrixSetValue(1, 1, 1);

    Matrix::Matrix B(2, 2);
    A.matrixSetValue(0, 0, 2);
    A.matrixSetValue(1, 0, 2);
    A.matrixSetValue(0, 1, 3);
    A.matrixSetValue(1, 1, 3);

    printMatrix(A);
    printMatrix(B);
*/













    return 0;    
}
